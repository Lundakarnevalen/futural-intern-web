<div id="left" class="col-md-8">
  <%= render partial: "shared/header", locals: {title: "Push-meddelanden"} %>

	<% @notifications.each do |n| %>
		<a href=<%= url_for n %>>
			<div class="message">
				<div class="title"><%= n.title %></div>
				<div class="notification_date"><%= n.created_at.strftime("%Y-%m-%d %H:%M") %></div>
			</div>
	<% end %>
</div>
