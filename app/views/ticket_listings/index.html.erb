<%= render 'shared/header', :title => 'Karneblocket' %>

<%= render 'filter_form', :query => @query %>

<table class='table table-striped'>
  <thead>
    <tr>
      <th>Händelse</th>
      <th>Pris</th>
      <th>Säljare</th>
      <th>Önskar</th>
    </tr>
  </thead>
  <tbody>
    <% @listings.each do |l| %>
      <%= render 'listing', :listing => l %>
    <% end %>
  </tbody>
</table>

