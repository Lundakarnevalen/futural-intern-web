<%= render partial: "shared/header", locals: {title: "Skapa nyhet"} %>

<% if current_sektioner.empty? %>
  <%= render "form", selected_section: 0 %>
<% else %>
  <%= render "form", selected_section: current_sektioner.first.id %>
<% end %>
