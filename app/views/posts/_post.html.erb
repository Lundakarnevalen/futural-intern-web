<div class="message col-xs-12">
  <%= image_tag("logotyper/#{post.sektion.name}.png", class: "img-responsive message-image col-xs-2") unless post.sektion.nil? %>
  <%= image_tag("logotyper/logo.png", class: "img-responsive message-image col-xs-2") if post.sektion.nil? %>
  <div class="message-information col-xs-8">
    <div class="row">
      <h1><%= post.title %></h1>
    </div>
    <div class="row">
      <div class="post-preview"><%= render_markdown post.content %>
      </div>
    </div>
  </div>
<% unless post.id.nil? %>
    <div class="row">
      <div class="col-xs-12 form-controls">
        <%= link_to "läs mer", post, class: "green button" %>
        <% if (current_user.can? :edit, post) %>
        <%= link_to "redigera", edit_post_path(post), class: "button", type: "edit" %>
        <% end %>
      </div>
    </div>
    <% end %>
</div>